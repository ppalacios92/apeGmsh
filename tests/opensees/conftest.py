"""Shared fixtures for the ``apeGmsh.opensees`` test suite.

Phase 0 ships no shared fixtures yet. Slice agents add fixture
modules under ``tests/opensees/fixtures/`` (one module per fixture
builder, immutable once committed — see
``architecture/testing.md`` § "Fixture sharing").
"""
